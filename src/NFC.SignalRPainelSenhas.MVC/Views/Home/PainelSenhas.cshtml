@{
    ViewBag.Title = "";
}
<h2>Painel de Senhas</h2>
<h3>Guinche: @Html.TextBox("tbGuinche", "")</h3>
<h3>Senha: @Html.TextBox("tbSenhaAtual", "")</h3>


@section scripts {

    @Scripts.Render("~/bundles/SignalR")
    <script src="~/signalr/hubs"></script>

    <script>

        $(function () {
            var painel = $.connection.painelSenhasHub;
            painel.client.dispararSenhaAtual = function(guinche,senha) {
                $("#tbGuinche").val(guinche);
                $("#tbSenhaAtual").val(senha);

            };

            $.connection.hub.start();
        });

    </script>
}

